{
  "name": "jt_graph_start",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ jt_graph_start",
    "create-local": "graph create --node http://localhost:8020/ jt_graph_start",
    "remove-local": "graph remove --node http://localhost:8020/ jt_graph_start",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 jt_graph_start"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.1",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
